## Usage

```shell
1、git clone https://github.com/WhiteMatrixTech/movecastle-sui-demo-frontend.git
2、cd movecastle-sui-demo-frontend
3、update contract address in src/utils/const.ts
4、npm install serve -g
5、npm install && npm run build && serve -s dist
6、port forward 
```
